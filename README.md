# AWS Lambda function to Start and Stop instances</br>
Input json format having AWS region name and instance Id </br>
{
  "region": "region-name",
  "instances": [
    "instance-Id",
    "instance-Id"
  ]
}
